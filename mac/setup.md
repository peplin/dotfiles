* Install homebrew
* Install Hack font
* Install gruvbox theme in Terminal.app
    https://github.com/sidnvy/gruvbox-terminal

xcode-select --install
brew install \
    coreutils \
    bash \
    the_silver_searcher \
    direnv
